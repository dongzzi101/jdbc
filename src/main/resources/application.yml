spring:
  datasource:
    url: jdbc:h2:mem:~/testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: none

  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
